[project]
name = "fastapi-app"
version = "0.1.0"
description = "A simple FastAPI app"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic",
    "pytest",
    "pytest-asyncio",
    "httpx",
    "pytest-cov",
]